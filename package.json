{
  "name": "firefox-seo-inspector",
  "version": "0.2.0",
  "private": true,
  "description": "Source, tests, and deterministic build tooling for Firefox SEO Inspector.",
  "scripts": {
    "lint": "node scripts/static-check.mjs && node scripts/manifest-check.mjs && node scripts/privacy-scan.mjs",
    "test": "node --test tests/*.test.cjs",
    "build": "node scripts/build.mjs",
    "verify:build": "node scripts/verify-build.mjs",
    "check": "npm run lint && npm test && npm run build && npm run verify:build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
